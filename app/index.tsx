import React from 'react';
import RootLayout from './_layout';

// The index file in the app directory is the root route.
// It renders the RootLayout which contains all the providers and the main app component.
export default function Index() {
  return <RootLayout />;
}